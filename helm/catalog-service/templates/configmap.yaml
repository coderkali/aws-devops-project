apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-catalog-config
  namespace: {{ .Release.Namespace }}
data:
  DB_ENDPOINT: "{{ .Values.db.host }}:3306"
  DB_READ_ENDPOINT: "{{ .Values.db.host }}:3306"
  DB_NAME: "catalog"
